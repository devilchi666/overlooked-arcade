# Theming Substrate — Roadmap

ARC 1 of the theming-substrate arc. Plan locked at
[docs/PLANS/theming-substrate.md](../../PLANS/theming-substrate.md).
Plan §6 holds the full phase breakdown (acceptance gates, critical
files, decisions). This roadmap is the slice-by-slice status board.

## ARC 1 phase plan

| Phase | Scope | Branch | Status |
| --- | --- | --- | --- |
| 1 | Engine/Theme surface separation. Extract Settings + Library Manager + Import Wizard + BIOS + Core installer + System Health + Background Jobs into engine-owned fullscreen-takeover surface summoned via `F12` / `Select+Start` / top-right corner icon. Retroverse drops from 6 → 5 tabs. Write SURFACES.md. | `feat/theming-substrate-phase-1` | ✅ shipped 2026-06-06 (in `engine/`, `platform/engineSurface.ts`, `platform/dialogs.ts`, `engine/SettingsPanel.tsx` + App.tsx wiring; SETTINGS dropped from `routing/currentRoute.ts`; SettingsPage.tsx deleted; 744 oa-shell tests pass; typecheck silent) |
| 2 | Platform / Theme SDK foundation. Move shared layer to `frontend/src/platform/` (`@oa/platform` alias). Define `theme.toml` manifest schema + `ThemeContext` interface. Rename Retroverse* → Theme* generics. ESLint boundary rule. Cleanup `HOTSPOT_SYSTEMS` triplicate + `customComponent` orphan. | (multi-slice) | ✅ Slice A shipped 2026-06-06. Slice B shipped 2026-06-07 (stores + lib + themes moves: `platform/{lib,themes,settings,library,layout/state,views}/` with all importers rewritten to `@oa/platform/*` alias; `SidebarView` type extracted to `platform/layout/types.ts`; ~130 import sites rewritten). Slice C shipped 2026-06-07 (13 component moves into `platform/components/` — the 6 declared + the 7-file private sub-component cluster per operator decision; `RetroverseContext` → `ThemeContext` / `useRetroverse()` → `useTheme()` rename; `ThemeManifest` type in `platform/theme/manifest.ts`; 790 tests pass; typecheck silent; awaiting operator playtest before merge). Phase 2 complete modulo the ESLint boundary rule, deferred to Phase 4 by operator decision. Residual wrong-direction imports: ~~`platform/components/*` → `../../nav/{focus,back,HintBar}`~~ **CLOSED in Phase 3 S1** (nav relocated to `platform/nav/`; importers repointed to `@oa/platform/nav`) + SystemHeader → `../../components/SystemCoresStrip` (core-installer surface, engine territory). |
| 3 | Theme substrate — **resequenced skeleton-first 2026-06-10** (plan §13.3; DECISIONS D19/D20). Execution order is S1 nav foundation → S2 walking skeleton (Retroverse + rough Wheel switchable — the swap gate) → S3 token layer (+ `THEME_CONTRACT.md`) → S4 versioned manifest + validator → S5 substrate depth (palette JSON, asset resolver, glyph seam, `wheel`/`carousel`/`custom`). Per-system theming is Retroverse-only, not a substrate contract (D19); kiosk/cabinet capabilities are deferred platform features with two seams reserved (D20). | `feat/theming-nav-foundation` | 🔄 **S1 (nav foundation) ✅ shipped + merged 2026-06-10 (operator playtested).** Relocated `src/nav/` → `platform/nav/` (closes the Phase-2 residual wrong-direction edges); verb vocabulary (`verbs.ts`) + input→verb indirection (`navBindings.ts`, OA-wide, `nav_bindings.json` persistence via `platform/api/navBindingsApi`); `focus.ts` + `HintBar` now dispatch/render by **verb** (A/B swap collapsed into a binding); glyph-set seam (`glyphs.ts`); `list`/`grid` declarative-prop primitives (`primitives/`); keyboard arrow-nav wired; new `platform/nav ↛ platform/components` lint zone. 822 oa-shell tests pass; typecheck + lint green. **S2 (walking skeleton) ✅ shipped + merged 2026-06-10 (operator playtested; `feat/theming-walking-skeleton`, merge `1fbec3e`, DECISIONS D22):** minimal Theme SDK contract (`platform/theme/{types,host,registry}`); host context relocated to platform (move+re-export); active-theme registry (platform owns machinery, App injects `BUILTIN_THEMES`); `activeThemeId` on `LibraryPrefs`; restart via new `restart_app` (`AppHandle::restart()`); Retroverse wrapped as default theme (thin wrapper); rough **CoverFlow** coverflow 2nd shell (system-agnostic, on `useFocusGroup` + windowed track; renamed from "Wheel" — true radial Wheel is the S5 `wheel` primitive); `EngineSummonIcon` → `platform/components/` (D12); `themePreempted()` D20a seam; `surfaces` manifest field (D20b); theme mount isolated in its own stacking context; Appearance picker filled into the Themes Settings category; 6 new lint zones. 822 tests pass; typecheck + lint green. Playtest round-1 fixes: z-index isolation, focus-claim, windowing, mouse/scroll. **S3 (token layer) ✅ shipped + merged 2026-06-10 (operator playtested; `feat/theming-token-layer`, merge `340c3fe`, DECISIONS D23):** typed `ThemeTokens` contract (`platform/theme/tokens.ts`) formalizing the existing `index.css` CSS-var system (palette/typography/geometry; motion reserved for ARC 2); theme `tokens` scoped-injected on the S2 theme-mount wrapper so engine territory always reads `:root` (D2 guarantee = structural, sibling-scope); a11y `prefers-reduced-motion` baseline + `--oa-focus-ring` token; CoverFlow re-skinned via a cool token set (proves same-code-different-tokens); `THEME_CONTRACT.md` written (theme-facing peer of SURFACES.md). Preceded by a BigBox research round confirming the cinematic/motion axis = ARC 2-3 (operator kept S3 static). typecheck + lint green; frontend-only. **S4 (versioned manifest + validator, `bare` fixture) ✅ shipped + merged 2026-06-10 (`feat/theming-manifest-validator`, merge `6fb0653`, DECISIONS D24; operator playtested):** pure `validateTheme(pkg)` (`platform/theme/validate.ts`) over the declarative manifest+tokens surface (required fields / `schema_version` ∈ supported-set `{1}` / `surfaces` ⊆ `["main"]` / `required_engine_capabilities` ⊆ engine-advertised `[]` / token keys ∈ `ThemeTokens` + non-empty values); registry validates-on-register, excludes invalid themes from the picker + `activeTheme()`, fallback toast when a persisted id is no longer valid; `bare` minimal theme (`themes/bare/`, in `BUILTIN_THEMES`) as picker-selectable floor + validator fixture; **Vitest stood up** (frontend's first test runner — `vitest` + `jsdom` + `vitest.config.ts` + `npm run test` in CI; 25 tests across `validate.test.ts` + `builtin-themes.test.ts`); THEME_CONTRACT.md §6 rewritten (enforced/structural/deferred). The no-`:root` rule stays structurally-backed (sibling-scope + boundary lint), not runtime-enforced (D24). typecheck + lint + test + build green; frontend-only. **S5 (substrate depth) — sliced into 5 per-sub-area micro-slices (operator choice; contracts first).** **S5.1 (resolver theme tier) ✅ shipped + merged 2026-06-11 (`feat/theming-s5-1-resolver-theme-tier`, merge `783da2e`, DECISIONS D25):** `resolve_background_asset` + `resolve_ui_sound` gain a leading `themeId` param + walk a shared theme→platform cascade (`system_ui_assets::candidate_asset_bases`; theme/<system> → theme/_baseline → system/<system> → system/_baseline, ui-sound keeps operator-override on top); theme overrides home at `<exe_dir>/assets/themes/<id>/system-ui/…` (operator-droppable now, no Phase-5 loader); frontend api wrappers take `themeId`, dispatchers resolve `activeThemeId()` ambiently (zero consumer churn). 830 oa-shell tests (+8 cascade) + typecheck/lint/vitest/build green. Merged on the test basis — the **background** consumer `SystemBackground` is unmounted since 2026-05-31, so reviving a theme-owned background surface **folds into S5.5**; the **ui-sound** half is live (grid-nav sounds). **S5.2 (palette substrate) ✅ shipped + merged 2026-06-11** (`feat/theming-s5-2-palette-substrate`, merge `f5b9b61`, DECISIONS D26): retired hand-authored `themes/systems.css` for a typed `SYSTEM_PALETTES: Record<SystemId, SystemPalette>` single source (`platform/themes/systemPalettes.ts`; glow derived as accent@0.35α), with the global `[data-system]` baseline derived + injected at boot (`ensureSystemPaletteBaseline()`, index.tsx, no flash); per-theme `perSystemTokens` scoped override seam (App.tsx injects `.oa-theme-mount [data-system="x"]{}` so a theme beats the baseline inside its mount while engine territory keeps it — D19 sub-cascade, D2 sibling-scope); validator extended (`UNKNOWN_SYSTEM_ID`/`UNKNOWN_PALETTE_KEY`/`EMPTY_PALETTE_VALUE`); THEME_CONTRACT.md §4/§6 updated. Data-home = typed map, NOT `config/*.json` + a build step (frontend-only data, no Rust reader). `bare` reframed as the substrate **test bed** + the seam's live consumer (per-system accent dots + a scoped NES→cyan/PSX→magenta demo). typecheck + lint + vitest(37, +12) + build green; frontend-only. **S5.3 (glyph-set seam) ✅ shipped + merged 2026-06-11** (`feat/theming-s5-3-glyph-set`, merge `af13cb7`, DECISIONS D27): made the S1 verb→glyph indirection theme-choosable — `PLAYSTATION_GLYPH_SET` + `GLYPH_SETS` registry + `activeGlyphSet()` signal (HintBar paints through it reactively); loose manifest `glyph_set` field bridged from App (`setActiveGlyphSetId`, mirrors S1 settings→nav bridges); validator `UNKNOWN_GLYPH_SET` warning (cosmetic, falls back to xbox); `bare` ships `glyph_set: "playstation"` as the live consumer. Picker + auto-detect deferred (#4). vitest 45. **S5.4 (per-theme settings namespace) ✅ shipped + merged 2026-06-11** (`feat/theming-s5-4-theme-settings`, merge `895f8c0`, DECISIONS D28): collision-free per-theme prefs (`platform/theme/themeSettings.ts` + `useThemeSettings()` auto-bound to the active theme's id → a theme can only touch its own slice); one localStorage key (`oa.themeSettings`), reactive `createStore`, survives the restart swap; `bare`'s header "Compact" toggle is the live consumer; THEME_CONTRACT.md §7 (fourth namespace). vitest 49. **S5.5 (primitives + background revival) ✅ shipped + merged 2026-06-11** (`feat/theming-s5-5-primitives`, merge `105fad8`, DECISIONS D29): `CarouselNav` (CoverFlow dogfooded onto it — bespoke windowing deleted) + `CustomNav` escape hatch + reserved `WheelNav` contract + `onNavSound` hook (#6, engine default `navSoundDispatcher`) + `ThemeBackground` (revived dead `SystemBackground` as a theme-opt-in backdrop → the S5.1 background tier's live consumer, CoverFlow mounts it); shared `useLateClaim` + ListNav/GridNav scroll-into-view fell out of the 3-round playtest. vitest 51. **✅ S5 COMPLETE — the Phase-3 substrate-depth arc is closed.** **D18 nav-remap Settings UI (gamepad) ✅ shipped + merged 2026-06-11** (`feat/theming-nav-remap-settings`, merge `2b40e3e`, DECISIONS D30): `NavRemapCard` in Settings → Controls rebinds the shell-nav verbs to standard-layout gamepad buttons (live, no restart; conflict-by-steal; per-row + global Reset; F12/keyboard escape hatch); pure `rebindGamepadVerb` + tests (vitest 58). The **keyboard** nav-remap UI + a real default keyboard map + the future per-controller-id gameplay-binding auto-config are queued in **PARKING_LOT.md** (the keyboard dispatch infra is already wired; only the editing UI is deferred). **Phase 3 fully closed.** Remaining ARC-1: original §6 Phase 5 (`.oatheme` distribution/loader) + Phase 6 (full Retroverse-as-theme move). |
| 4 | Tauri bridge hardening. Drain ~150 `invoke()` leakage sites into `platform/api/` domain modules. SystemId parity test (CI). Hand-written typed wrappers (defer tauri-specta). | (Slices 1-6 + 4.5) | ✅ **COMPLETE 2026-06-10.** 14 typed `platform/api/` modules; invoke ban + event ban lint rules on; six file-boundary zones. The platform/theme decoupling track is closed on both backend-contract channels (commands + events). See ACTIVE_WORK + SESSION_LOG. (SystemId parity test still ⬜ — folds into a later slice.) |
| 5 | `.oatheme` distribution + loading. Zip + loose-folder dev mode. Rust loader (`theme_loader.rs`). Theme picker UI (Manager → Appearance). Conflict + fallback policies. Build-time bundling only (runtime load deferred to ARC 2). | TBD | ⬜ pending |
| 6 | Retroverse rebuilt + 2nd pilot theme. **ARC 1 ACCEPTANCE GATE.** Retroverse-as-theme (same UX) + Wheel pilot (BigBox-inspired coverflow) proves SDK supports >1 IA shape. Both ship in binary. Operator switches in Appearance. | `feat/theming-retroverse-as-theme` | ✅ **2nd pilot (CoverFlow) landed early in S2** + deepened S3-S5. **Retroverse-as-theme ✅ shipped + merged 2026-06-11 (merge `711f337`; operator playtested — indistinguishable)** (DECISIONS D31): moved Retroverse from the S2 thin wrapper into a real theme under `themes/retroverse/` (RetroverseShell + 8 route files + theme-private `currentRoute.ts`), consuming only platform; **removed the last two `except: ['./retroverse']` boundary exceptions** (`themes↛routes` + `themes↛layout`, probe-verified). Reverse-import audit found zero files needing to hoist to platform (S2/Phase-4/grab-bag already hoisted everything shared) → pure relocation + `context.tsx` shim deletion + dead-code removal (`StubPage`, App.tsx `__retroverse_debug`). 3 green sub-commits; typecheck+lint+vitest(58)+build green; cargo `oa-shell` green. **Only Phase 5 now remains open in ARC 1.** |

## Gate from Phase 2 → Phase 3 (sequencing pause)

Per plan §7, ARC 1 pauses at end of Phase 2 to let the Virtual
Library arc's Phase E (game_identities schema, ~3-4 weeks) +
Phase C (Launcher trait, ~2-3 weeks) land. Resume Phase 3 after
both VL phases ship.

## Gate from ARC 1 → ARC 2

Per plan §11:

- All 6 phases' acceptance gates green
- `cargo test -p oa-shell` green
- `npm run typecheck` + `npm run lint` + `npm run test` (theme-contract gate, S4) silent/green
- SystemId parity test green
- Operator dogfood: Retroverse + Wheel switchable; Retroverse
  feels indistinguishable from pre-arc behavior; Wheel boots /
  browses / launches
- Third hand-built theme folder dropped at `<exe_dir>/themes/test/`
  → appears in Appearance picker → loads

ARC 2 (Per-System Layout Substrate — D32 per-system layout/view +
the `.oatheme` loader; declarative, no scripting) gets its own plan
when scheduled. The Behaviors + Shaders work (Rhai + WGSL) is now
ARC 3 (D35 renumber); KIOSK_PLAN §2.2 is its source spec and doesn't
need to be written from scratch.
