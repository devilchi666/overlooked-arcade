# Documentation Staleness Audit — 2026-06-15

Read-only survey of the **active doc surface** (project-wide root docs + active
feature folders + active plans). Four parallel audit agents; every code-path /
link claim verified against the live tree. Per-core `docs/cores/<id>/ROADMAP.md`
drift is a **deferred second pass** (not covered here).

This is a transient working tracker (precedent: the removed
`cores/AUDIT_2026-05-21.md`) — delete once findings are migrated back into their
docs. Checkboxes double as the fix list.

## Execution status (2026-06-15)

**✅ APPLIED this pass (batches 1–6 + deletions):**
- §A status flips (controller-identity, hw-render, guided-setup, VL Phase B/C,
  unified-nav) + the ACTIVE_WORK theming 4-arc header + Metadata stream closure.
- §B all dead-link repoints.
- §C path repaths across active docs (frozen-history docs got a single header
  note instead of line-by-line rewrites).
- §D arc-renumber drift (theming README/ROADMAP/THEME_CONTRACT/SURFACES/BigBox
  research + theming-substrate.md prose body + hw-render + ACTIVE_WORK header).
- §E supersession banners (metadata-editing, kiosk Theme-Studio scope,
  PARKING_LOT two-shell, ACTIVE_WORK D19, NEXT per-system block).
- §F **7 plans archived** → `_archive/PLANS/` + INDEX + `_archive/INDEX.md`
  manifest updated. **Metadata = fully closed** → plan + feature folder archived,
  removed from ACTIVE_WORK in-flight + NEXT HIGH band.
- `CHATGPT_BRIEFING.md` **deleted** (operator call — not refreshed) + INDEX ref
  removed.

**✅ APPLIED (pass 3, 2026-06-15):**
- §G NEXT.md pipelined-sequence narrative **compressed** — the ~300-line
  2026-05-26 pipeline + the shipped/superseded "NEXT MAJOR ARC" blocks
  (Guided Setup / Per-System UI / Game Info Panel / Background-jobs /
  Per-track) collapsed to one "Shipped & superseded arcs (historical pointer)"
  section (1164→892 lines). The live Virtual Library block kept.
- §G ACTIVE_WORK.md **re-sorted** — Settings IA, Per-System Hub, Controller
  Identity, Metadata Curation moved out of "In flight" into a compressed
  Recently-completed block; the superseded Per-System Stage 1 entry replaced
  with an ARC-2 pointer; Retroverse-UI obsolete flag-toggle + Stage 2/3 lines
  corrected. (Unified-Nav stays in flight — Phases 2-4 genuinely open.)

**✅ APPLIED (pass 4, 2026-06-15) — per-core ROADMAP sweep:**
- All **43** per-core `docs/cores/<id>/` folders audited (ROADMAP.md + README.md)
  via 7 parallel agents. **29 files** repathed for the platform/ refactor
  (`themes/systems.css` retired → `platform/themes/systemPalettes.ts`;
  `themes/registry.ts` → `platform/themes/`; `components/*` → `platform/components/`
  or `engine/`; dropped a stale `SystemBindingsEditor.tsx:226` line ref; fixed a
  `sega32xcd/README` relative-link depth). **Zero status flips** — every open ⬜
  is genuinely operator-validation / playtest / DATA-curation / deferred, not
  code-shipped-but-unmarked (verified: cross-system features that close per-core
  items were already ✅). No "suspected shipped" stragglers. SESSION_LOG /
  DECISIONS / KNOWN_GAME_BUGS left as frozen history.
- Note: the 2026-05-21 audit-doc footer the per-core ROADMAPs once carried is
  already gone (no dead references found) — memory pointer to it is stale.

**The active-surface + per-core audit is now complete.** This tracker can be
deleted once reviewed.

**Two conflicts resolved by direct git/tree check (so the audit states fact):**
- **Per-System Settings Hub IS merged to main** (merge `62c2344`; `engine/systemsHub/`
  + `GameMetadataEditor`/`PlatformMetadataEditor` present on main). ACTIVE_WORK is
  correct; an agent's "stranded on branch" read was wrong.
- **The standalone metadata Settings category UI was absorbed into the hub.**
  `MetadataSettingsBody.tsx` exists nowhere; the editor is now the hub's
  Game/Platform-metadata domain cards.

---

## The five root causes

1. **The `platform/` refactor wake** — ARC-1 moved `frontend/src/components/` →
   `platform/components/` + `engine/`; `src/nav/` → `platform/nav/`;
   stores/lib/themes → `platform/`; `routes/retroverse/` + `layout/retroverse/`
   → `themes/retroverse/`; `systems.css` retired → `platform/themes/systemPalettes.ts`;
   `SystemBackground` → `ThemeBackground`. Dozens of docs still cite the old paths.
2. **D35 arc renumber (this session)** — "ARC 2 = Rhai/WGSL" / "Theme Studio
   (ARC 3)" / "3 arcs" are now off-by-one (→ ARC 3 / ARC 4 / 4 arcs).
3. **Shipped-but-listed-as-pending** — several READ-ME/plan headers never got
   flipped after the work merged. The most dangerous category (a fresh session
   could rebuild shipped work).
4. **Superseded architecture stated as live** — per-system "default + global
   toggle"/D19; metadata-editing's standalone category; kiosk owning Theme Studio.
5. **Link rot** — plans/feature folders that moved to `_archive/`.

---

## A. HIGH — actively misleading (status drift on shipped work)

- [ ] `features/controller-identity/README.md:9-10` — "Not yet started in code." → the **full arc shipped + merged 2026-06-13** (`808fc0b`). Rewrite status; list parked follow-ups.
- [ ] `features/hw-render/README.md:49-52` — "No code yet." → **M1 proven, M2 merged** (`c27da4c`, tag `hw-render-m2-proven`). M3 stranded on `feat/hw-render-m3`; M4 future. Also `:21-24` integration points marked "to add" are implemented.
- [ ] `PLANS/guided-setup.md:3` — "Status: Planning. No code." → Phase 0/1B + wizard + readiness checklist + Phase-2 CPU-tier curation shipped. **Verify each phase against code before marking**; rewrite header.
- [ ] `PLANS/controller-identity-substrate.md:3-5,239-266` — header "execution queued"; Phases 3-6 read as future though the arc closed. Reconcile shipped-vs-rolled-into-follow-on, then archive (§F).
- [ ] `PLANS/virtual-library-and-launcher-arc.md:426-477` — Phase C reads as future; it's **merged** (`28875d5`). Mark ✅. `:385-424` Phase B has no status → add "code-complete, stranded on `feat/virtual-library-phase-b`, not merged."
- [ ] `PLANS/unified-navigation-and-panels.md:1,88-103` — no status line, reads not-started; **Phase 1 merged** (`c372dd2`). Add status block; mark Phase 1 ✅.
- [ ] `ACTIVE_WORK.md` "In flight" — Settings IA (closed), Per-System Hub (merged), Unified-Nav Phase 1 (merged), Controller Identity (shipped) all still sit under the **In flight** heading with inline ✅. Move closed streams to "Recently completed"; keep only genuinely-open work in flight.
- [ ] `ACTIVE_WORK.md:121-130` — "Metadata Curation" listed as a separate in-flight stream with its own Settings category. Its UI was **absorbed into the Per-System Hub** (verified). Reconcile: mark absorbed; note any residual game-factual override work (see §E).
- [ ] `CHATGPT_BRIEFING.md` §7 dir map (534-586) + §3.3 (164-183) — directory map + per-system-UI model both pre-refactor / pre-D32. Whole-file refresh (§G).

## B. Dead links (repoint to `_archive/` or moved target)

- [ ] `NEXT.md:249` — `PLANS/game-info-panel.md` → `_archive/PLANS/game-info-panel.md`.
- [ ] `SESSION_LOG.md:221` — `PLANS/settings-declutter-system-health.md` → `_archive/PLANS/`.
- [ ] `features/retroverse-ui/README.md:13-21` — 4 tab plans (`settings-/play-now-/discover-/collections-tab-retroverse.md`) → `../../_archive/PLANS/`.
- [ ] `features/guided-setup/README.md:18` — `../controller-nav/` → `../../_archive/features/controller-nav/`.
- [ ] `features/kiosk-shell/README.md:27,29` + `KIOSK_PLAN.md:3,9,527,634-635` — `../sidebar/`, `../ui-polish/*` → `../../_archive/features/...`.

## C. Stale code paths (the `platform/` refactor wake) — verified moves

Mechanical repath. Highest-impact (load-bearing "lean on this" surfaces) first:

- [ ] `NEXT.md:1192` (Game Info Panel infra) — `frontend/src/components/TouchHotspotOverlay.tsx` → `platform/components/`; "Retroverse `GameDetailPanel`" → `themes/retroverse/GameDetailPanel.tsx`.
- [ ] `NEXT.md:1184` (infra inventory) — "Per-system theming — `frontend/src/themes/systems.css`" → `platform/themes/systemPalettes.ts` (systems.css retired, D26).
- [ ] `features/retroverse-ui/README.md:27-49` — whole "Where the code lives" block is pre-refactor (`layout/retroverse/`, `routing/`, `routes/retroverse/`, `components/`, `library/`, `lib/`). Rewrite to `themes/retroverse/` + `platform/` + `engine/`. Remove deleted `PerSystemSettingsBody.tsx` / `systemMetadataStubs.ts` / `context.tsx` rows.
- [ ] `PLANS/retroverse-ui-rollout.md:50-52` (`frontend/src/layout/` deleted → `themes/retroverse/RetroverseShell.tsx` + `platform/components/LeftSidebar.tsx`) + `:58-60` (`themes/systemUIConfigs.ts`/`registry.ts` → `platform/themes/`).
- [ ] `PLANS/virtual-library-and-launcher-arc.md:367-372,592-594` — `components/{LibraryTile,LibraryGrid,GameDetailPanel}`, `routes/retroverse/SystemHealthPage` → `platform/components/`, `platform/components/VirtualLibraryGrid.tsx`, `themes/retroverse/`, `engine/SystemHealthPage.tsx`.
- [ ] `PLANS/game-identities-schema.md` (~366-370) — `components/LibraryTile`/`GameDetailPanel` → platform/themes homes.
- [ ] `features/per-system-ui/ASSETS.md:50,302` + `DECISIONS.md:9-11,32,42-43,64` — `frontend/src/themes/{registry,systemUIConfigs}.ts` → `platform/themes/`; `reducedMotion.ts` → `platform/lib/`; `<SystemBackground>` → `<ThemeBackground>` (add "now theme-opt-in"). Use "(now `platform/…`)" annotations on the append-only DECISIONS, don't rewrite history.
- [ ] `PLANS/per-system-descriptors.md:39,328` + `PLANS/dynamic-controller-info.md:11` + `PLANS/metadata-editing.md` (spot-check) — `themes/registry.ts`→`platform/themes/`, `components/GameDialogs.tsx`→`platform/components/`.
- [ ] `PLANS/dynamic-input-descriptors.md:30` — `crates/oa-pce-sys/vendor/.../libretro.h` (retired crate) → `crates/oa-libretro/`.
- [ ] `features/kiosk-shell/README.md:6` + `KIOSK_PLAN.md §20:601` + `features/dosbox-and-scummvm/README.md:162-175,244,248` — `themes/systems.css`/`registry.ts`, `components/SystemHeader`, `library/ingest` → platform homes. (DOSBox doc is a frozen shipped design; a one-line "paths predate the platform/ refactor" header note may suffice.)
- [ ] `features/guided-setup/SESSION_LOG.md` (many) — `components/import-wizard/` → `engine/import-wizard/`. Frozen history → a single header note is enough.
- [ ] `features/theming-substrate/ROADMAP.md:12` (`engine/manager/SettingsPanel.tsx` → `engine/SettingsPanel.tsx`); SURFACES.md path notes (§E).

## D. D35 arc-number drift (off-by-one; I created this — should fix)

- [ ] `PLANS/theming-substrate.md:43-44,74-75,100-109,584` — prose still "3 arcs / Arcs 2-3 Rhai+WGSL+Theme Studio" (§5 table already fixed). → "4 arcs"; Rhai/WGSL = ARC 3, Theme Studio = ARC 4.
- [ ] `features/theming-substrate/README.md:44-48,47,56-60,66-75` — 3-arc table + "Queued. No code yet." → 4-arc table (from plan §5) + "ARC 1 shipped bar the .oatheme loader"; reframe the per-system "paused side-stream" line per D34.
- [ ] `features/theming-substrate/ROADMAP.md:40-41` — ARC-1→ARC-2 gate text names the old ARC 2 (Rhai+WGSL). → "ARC 2 = Per-System Layout Substrate."
- [ ] `features/theming-substrate/THEME_CONTRACT.md:150-155` — "motion … ARC 2/2-3" → ARC 3.
- [ ] `features/theming-substrate/SURFACES.md:146-148` — layer-contract table lists `routes/retroverse/**`+`layout/retroverse/**` as the *current enforced* theme dirs; they're deleted → `themes/retroverse/**` / `themes/**`. (`:45-53,82` "Today's location" column is explicitly Phase-1 snapshot — add a one-line disclaimer.)
- [ ] `features/theming-substrate/BIGBOX_RESEARCH_2026-06-11.md` — pervasive 3-arc numbering (lines 8-9,40,53-54,88,192,203,224,246-293,320,355,378-382). Bulk-renumber (lower-traffic research doc; MED).
- [ ] `features/hw-render/README.md:52` — "before Theming ARC 2" → now ARC 3 (the cinematic arc it meant). Or drop the number.
- [ ] `ACTIVE_WORK.md:133-138` — theming stream header still "3 arcs; ARCs 2-3 (Rhai+WGSL+Theme Studio)" (contradicts its own ARC-2 body). → 4-arc framing.

## E. Superseded architecture stated as live

- [ ] `features/metadata-editing/` (README + DECISIONS D6-D9 + SESSION_LOG) — the dedicated `metadata` Settings-category / full-screen-takeover UI is **superseded by the Per-System Hub** (editor now = `engine/systemsHub/domains/{Game,Platform}MetadataEditor.tsx`). Backend decisions (D1-D5, `game_metadata_overrides` schema) still valid + reused. → Supersession banner pointing to `features/per-system-hub/`; reconcile the ACTIVE_WORK stream (§A). **Operator call:** is any standalone game-factual editor work still planned, or is this folder fully closed?
- [ ] `features/kiosk-shell/KIOSK_PLAN.md §2.2-2.4` + README:6 — positions kiosk as owning the theme substrate + Theme Studio. That scope **migrated to `features/theming-substrate/` (ARCs 1-4, D7)**. → Banner: substrate/Theme-Studio scope moved; kiosk-specific bits (attract/multi-monitor/5-bus mixer) are the D20 deferred platform capabilities.
- [ ] `PARKING_LOT.md:149-177` (2026-05-31 two-shell + Stage-2 routing) — frames "Kiosk hosts the themable experience" as the home for `SystemBackground`/Stage-2; superseded by the swappable-whole-shell substrate + `ThemeBackground` (D29) + D32-34. → Supersession note (append, don't rewrite); paths dead.
- [ ] `ACTIVE_WORK.md:304` — records D19 "per-system theming Retroverse-only" as current; superseded by D32/D33. → Annotate.
- [ ] `NEXT.md:218-243` (Per-System Custom UI arc block) — "default OA experience + global master toggle" (D32/33/34-superseded); contradicts the HIGH-band L1 entry. → Strike/annotate.
- [ ] `CHATGPT_BRIEFING.md §3.3,§5,§3.10` — per-system default-toggle model + "theme ecosystem deferred" + no mention of the shipped theming substrate (§G refresh).

## F. Archive candidates (fully shipped plans → `_archive/PLANS/`)

Recommend move; operator confirms. Parent arcs retain the summary.

- [ ] `PLANS/theming-platform-api-bridge.md` — Phase 4 complete (invoke+event bans live in `eslint.config.mjs`).
- [ ] `PLANS/theming-grabbag-drain.md` — shipped 2026-06-09 (`src/components/` gone).
- [ ] `PLANS/controller-identity-substrate.md` — full arc merged 2026-06-13.
- [ ] `PLANS/settings-ia-redesign.md` — Slices 1-4 merged; Slice 5 deferred into guided-setup Phase 2 (note in guided-setup before archiving).
- [ ] `PLANS/launcher-abstraction.md` — VL Phase C sub-plan; C1-C3 merged.
- [ ] `PLANS/game-identities-schema.md` — VL Phase E sub-plan; sub-phases 1-3 shipped (repath §C first).
- [ ] `PLANS/disc-track-sha1-matching.md` — VL Phase A1 sub-plan; shipped + pivoted; only deferred Sub-phase 4 parked.

## G. Structural / larger refactors (own batch — more judgment)

- [ ] `CHATGPT_BRIEFING.md` (whole file, self-dated 2026-06-01) — **most stale doc.** §7 dir map wrong; §3.3 per-system model superseded; no theming-substrate mention; §9 stats stale (test count, etc.). Recommend a full refresh pass *after* the mechanical fixes land (so it can be rebuilt from corrected sources). Larger effort — its own session-chunk.
- [ ] `NEXT.md` pipelined-sequence narrative (lines 11-276) + the three "NEXT MAJOR ARC" blocks — describe a 2026-05-26 plan whose arcs have all shipped/superseded. The HIGH band is current; the top ~280 lines read as upcoming. → Compress to a short "shipped — see history" pointer; archive the narrative.
- [ ] `ACTIVE_WORK.md` In-flight/Recently-completed re-sort (§A) — structural; pairs with the NEXT.md cleanup.

---

## Proposed fix batches (for sign-off)

| Batch | Content | Risk | Recommend |
| --- | --- | --- | --- |
| **1 — Links** | §B dead-link repoints | none | fix now |
| **2 — Status flips** | §A header/status corrections (verify-then-flip) | low | fix now |
| **3 — Arc renumber** | §D off-by-one (the drift I introduced) | low | fix now |
| **4 — Paths** | §C repaths (active surfaces; frozen-history docs get a header note not a rewrite) | low | fix now |
| **5 — Supersession banners** | §E banners/annotations (append-only; metadata-editing needs the operator call) | low–med | fix now bar the metadata-editing question |
| **6 — Archive** | §F move 7 shipped plans to `_archive/PLANS/` + update INDEX/_archive manifest | med (file moves) | confirm, then fix |
| **7 — Structural** | §G CHATGPT_BRIEFING refresh + NEXT/ACTIVE_WORK restructure | med (rewrites) | separate pass |

Per-core ROADMAP drift = a deferred second audit pass (not in this report).
