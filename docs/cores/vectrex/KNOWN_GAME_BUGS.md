# vectrex Known Game Bugs

Per-game compatibility issues on the configured Vectrex core (vecx by default). Append-only.

Format:

```
## <Title> (<Region>) — <Date>
- **Symptom:** <what user sees>
- **Trigger:** <what makes it happen>
- **Workaround:** <what to do — per-game core override, BIOS install, etc.>
- **Status:** <reported / confirmed / fixed upstream / works on alternate core>
```

---

(No entries yet. Phase 1 operator validation will populate.)

Likely categorical entries once validated:
- **3D Imager titles** (3D Mine Storm, 3D Narrow Escape, 3D Crazy Coaster) — the 3D Imager stereoscopic accessory has no perfect emulation; games run flat without the depth effect.
- **Overlay-aware games** — most Vectrex games used clip-on plastic overlays for color. Without overlay rendering (Phase 2 polish), the screen stays monochrome green.
