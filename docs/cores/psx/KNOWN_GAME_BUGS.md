# psx Known Game Bugs

Per-game compatibility issues observed on the configured PSX core
(Beetle PSX HW by default; Beetle PSX SW available as a catalog peer
alternate via per-system Cores; SwanStation also documented).

Append-only; newest at the bottom.

Format:

```
## <Title> (<Region>) — <Date>
- **Symptom:** <what user sees>
- **Trigger:** <what makes it happen>
- **Workaround:** <what to do — HW→SW swap, per-game core, region BIOS, etc.>
- **Status:** <reported / confirmed / fixed upstream / works on alternate core>
```

---

(No entries yet. Phase 1 operator validation will populate this file
with whatever per-game quirks show up. Expected hotspots: HW-renderer
games that need PGXP geometry correction disabled, Ape Escape requiring
DualShock analog sticks deferred to Phase 2, multi-disc swaps via .m3u
for FFVII/MGS, .pbp container edge cases.)
