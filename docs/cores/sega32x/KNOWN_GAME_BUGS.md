# sega32x Known Game Bugs

Per-game compatibility issues observed on the configured Sega 32X core
(PicoDrive by default; per-system Cores override applies, though
practical alternatives don't exist — PicoDrive is the only mainstream
libretro core with 32X support).

Append-only; newest at the bottom.

Format:

```
## <Title> (<Region>) — <Date>
- **Symptom:** <what user sees>
- **Trigger:** <what makes it happen>
- **Workaround:** <what to do — per-game region override, etc.>
- **Status:** <reported / confirmed / fixed upstream>
```

---

(No entries yet. Phase 1 operator validation will populate this file
with whatever per-game quirks show up.)
