# lynx — Known game bugs

Per-game compatibility issues as they surface during operator validation. Empty at Phase 0; populated as the reference set rolls in.

Format per entry:

```
## <Game title> (region / dump)

**Severity:** crashes / unplayable / playable-with-glitches / cosmetic
**Reproduce:** <minimal steps>
**Root cause:** <upstream link if known, "investigating" if not>
**Workaround:** <what the operator can do>
```

No entries yet.
