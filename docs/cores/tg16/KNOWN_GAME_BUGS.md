# tg16 — Known Game Bugs

Per-game compatibility issues. Add entries as we hit them.

Format per entry:

```
## <Game name> (region, format)
- **Symptom:** What goes wrong.
- **First seen:** YYYY-MM-DD, commit/version.
- **Repro:** Steps to reproduce.
- **Suspected cause:** What we think is happening (link to file/line if known).
- **Workaround:** None / per-game shim / config flag / etc.
- **Status:** Open / Under investigation / Fixed in <ref> / Won't fix (reason).
```

---

## (none yet)

Only one ROM tested so far — Bonk's Adventure (USA, HuCard) — and it runs cleanly. Real triage pass starts when we expand the test library.
