# neogeo Known Game Bugs

Per-game compatibility issues observed on the configured Neo Geo core
(FBNeo by default). Append-only; newest at the bottom.

Format:

```
## <Title> (<Region/Variant>) — <Date>
- **Symptom:** <what user sees>
- **Trigger:** <what makes it happen>
- **Workaround:** <what to do — Universe BIOS, AES vs MVS mode, etc.>
- **Status:** <reported / confirmed / fixed upstream>
```

---

(No entries yet. Phase 1 operator validation will populate this file
with whatever per-game quirks show up. Expected hotspots: titles
requiring Universe BIOS for region toggle, MVS-vs-AES mode-specific
behaviors, ROM-set hash mismatches between MAME revisions, content-peek
edge cases on hybrid .zip files.)
