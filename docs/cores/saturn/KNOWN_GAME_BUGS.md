# saturn Known Game Bugs

Per-game compatibility issues observed on the configured Saturn core
(Beetle Saturn by default; per-system Cores override applies — Kronos /
YabaSanshiro available as alternates).

Append-only; newest at the bottom.

Format:

```
## <Title> (<Region>) — <Date>
- **Symptom:** <what user sees>
- **Trigger:** <what makes it happen>
- **Workaround:** <what to do — per-game core override, BIOS region, cart RAM, etc.>
- **Status:** <reported / confirmed / fixed upstream / works on alternate core>
```

---

(No entries yet. Phase 1 operator validation will populate this file
with whatever per-game quirks show up. Expected hotspots: Capcom
fighters requiring 4MB cart RAM expansion, NiGHTS / Sonic R needing
analog stick support deferred to Phase 2, Panzer Dragoon Saga's
4-disc swap via .m3u.)
