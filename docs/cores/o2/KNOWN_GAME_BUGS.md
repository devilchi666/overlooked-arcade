# o2 Known Game Bugs

Per-game compatibility issues on the configured Odyssey² / Videopac core (O2EM by default). Append-only.

Format:

```
## <Title> (<Region>) — <Date>
- **Symptom:** <what user sees>
- **Trigger:** <what makes it happen>
- **Workaround:** <what to do — per-game core override, BIOS install, keyboard mapping, etc.>
- **Status:** <reported / confirmed / fixed upstream / works on alternate core>
```

---

## Keyboard-required games

Titles that REQUIRE alphanumeric keyboard input to progress (Phase 0 routes keyboard through OA's existing passthrough, same path as MAME — should work, but needs operator validation):

- **Quest for the Rings** — keyboard for character class selection at start.
- **Conquest of the World** — keyboard for territory commands.
- **The Great Wall Street Fortune Hunt** — keyboard for stock trading.
- **Type & Tell** — speech-synthesis trainer; entirely keyboard-driven.
- **Computer Programmer** — IDE; entirely keyboard-driven.

---

(No specific per-game bugs reported. Phase 1 operator validation will populate.)
