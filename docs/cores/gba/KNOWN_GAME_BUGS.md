# gba Known Game Bugs

Per-game compatibility issues observed on the configured Game Boy
Advance core (mGBA by default; per-system Cores override applies —
VBA-Next or VBA-M for lighter-footprint fallback).

Append-only; newest at the bottom.

Format:

```
## <Title> (<Region>) — <Date>
- **Symptom:** <what user sees>
- **Trigger:** <what makes it happen>
- **Workaround:** <what to do — per-game core override, BIOS install, etc.>
- **Status:** <reported / confirmed / fixed upstream / works on alternate core>
```

---

(No entries yet. Phase 1 operator validation will populate this file
with whatever per-game quirks show up. Note: a small set of GBA titles
require the real `gba_bios.bin` to boot — see README "BIOS" section.
Those go here once the operator encounters them.)
