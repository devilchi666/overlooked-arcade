# coleco Known Game Bugs

Per-game compatibility issues observed on the configured ColecoVision core (blueMSX by default; gearcoleco as the per-system Cores override).

Append-only; newest at the bottom.

Format:

```
## <Title> (<Region>) — <Date>
- **Symptom:** <what user sees>
- **Trigger:** <what makes it happen>
- **Workaround:** <what to do — per-game core override, BIOS install, etc.>
- **Status:** <reported / confirmed / fixed upstream / works on alternate core>
```

---

(No entries yet. Phase 1 operator validation will populate this file with whatever per-game quirks show up. Likely candidates: titles using the Super Action Controller spinner — Front Line, WarGames Star Wars — which won't be playable until shared analog-input infra lands.)
