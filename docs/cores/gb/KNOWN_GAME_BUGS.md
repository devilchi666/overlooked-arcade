# gb Known Game Bugs

Per-game compatibility issues observed on the configured Game Boy /
Game Boy Color core (Gambatte by default; per-system Cores override
applies — SameBoy for cycle-accurate fallback).

Append-only; newest at the bottom.

Format:

```
## <Title> (<Region>) — <Date>
- **Symptom:** <what user sees>
- **Trigger:** <what makes it happen>
- **Workaround:** <what to do — per-game core override, etc.>
- **Status:** <reported / confirmed / fixed upstream / works on alternate core>
```

---

(No entries yet. Phase 1 operator validation will populate this file
with whatever per-game quirks show up.)
